# SightMetrics – Sites dieses Stacks / sites of this stack
# Format: site_id<TAB>Logdatei<TAB>Name  (Tabulatoren, keine Leerzeichen)
# Die Logdatei schreibt der web-Container (Volume sightmetrics-logs, im
# Ingestion-Container unter /logs eingehängt).

1	/logs/access.log	GSB11
