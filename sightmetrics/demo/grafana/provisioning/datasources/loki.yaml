# Loki wird beim Grafana-Start automatisch als (Default-)Datenquelle angelegt,
# damit "Explore" die Logs sofort ohne manuelles Setup zeigt.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: loki
    url: http://loki:3100
    isDefault: true
    editable: true
