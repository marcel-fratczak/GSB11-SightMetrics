# CI variant: own composer install of the extension (vendor/ next to the config).
includes:
    - phpstan-base.neon
    - vendor/phpstan/phpstan-strict-rules/rules.neon
parameters:
    bootstrapFiles:
        - vendor/autoload.php
